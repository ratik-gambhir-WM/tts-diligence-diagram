This folder holds local mock diagram-template assets that should stay outside the Vite app source tree.

Keep `src/diagram_templates/diagram_react_flow` for app-consumed flow JSON.
Keep the rest of the template reference material here so it remains available locally without being bundled into the app build.
