This folder contains local prompt-context reference material that is intentionally kept outside `src/`.

These files can still be used as mock data or authoring references during local development, but they should not be bundled into the Vite app build.
